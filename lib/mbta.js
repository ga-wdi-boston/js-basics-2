'use strict';


const lines =

red = ['South Station', 'Park Street', 'Kendall', 'Central',
 'Harvard', 'Porter', 'Davis', 'Alewife']; {
   //naming the red line
green = ['Government Center', 'Park Street', 'Boylston', 'Arlington', 'Copley',
 'Hynes', 'Kenmore']
  //naming the green line
 orange = ['North Station', 'Haymarket', 'Park Street', 'State',
 'Downtown Crossing', 'Chinatown', 'Back Bay', 'Forest Hills'];
 //naming the orange line

let parkStIndexes = {
 red: 1;
 green: 2;
 orange: 2;
}
//naming the park street index in each line

  let totalStops = (startLine, startStation, endLine, endStation);
    //creating an array for the stops to go in
    for (let i = 0; i < redline.length; i++);
    //let the south station = 0, etc, until all are numbered, one by one
    if startLine = 

    return totalStops;
    //returns that array

};
